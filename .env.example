VITE_CLOUDBASE_ENV_ID=your-cloudbase-env-id
VITE_CLOUDBASE_PUBLISHABLE_KEY=your-publishable-key

# AI 总结：deepseek（默认）或 cloudbase
VITE_AI_BACKEND=deepseek
VITE_DEEPSEEK_MODEL=deepseek-chat

# DeepSeek API Key（仅本地/服务端，不要加 VITE_ 前缀，避免打包进前端）
DEEPSEEK_API_KEY=your-deepseek-api-key

# 若使用 CloudBase AI 而非 DeepSeek，改为：
# VITE_AI_BACKEND=cloudbase
# VITE_AI_PROVIDER=cloudbase
# VITE_AI_MODEL=deepseek-v4-flash
